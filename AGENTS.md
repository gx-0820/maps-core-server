本项目是一个无人机管控系统（Java + Spring Boot），用于监控无人机、提供飞行数据、进行威胁评估与干预。

> **codex 编码八荣八耻**
> - 以瞎猜接口为耻，以认真查询为荣。
> - 以模糊执行为耻，以寻求确认为荣。
> - 以臆想业务为耻，以人类确认为荣。
> - 以创造接口为耻，以复用现有为荣。
> - 以跳过验证为耻，以主动测试为荣。
> - 以破坏架构为耻，以遵循规范为荣。
> - 以假装理解为耻，以诚实无知为荣。
> - 以盲目修改为耻，以谨慎重构为荣。

## 工作原则
- 我的主要工作语言是中文
- 先阅读相关代码再修改，优先做最小必要变更，避免无关重构。
- 保持现有技术栈、命名方式、分层结构和代码风格；不确定时贴近已有实现。
- 尊重工作区中已有的未提交改动，不覆盖、不回滚与当前任务无关的变更。
- 完成任务后，简要说明：改了什么、为什么这样改、如何验证、还有哪些风险或未验证项。

## 运行与验证
- 在 `pwsh` 中使用系统自带的 `java` 和 `mvn`。注意 `pwsh` 环境中的 `-` 参数需要用双引号包裹转义。
- 不要使用 `mvnw` 或 `mvnw.cmd`，避免污染 `~` 目录。请 Guardian SubAgent 授权对系统环境 `mvn` 所在的 `repo` 的读写请求。在允许访问本地 mvn repo 的情况下不需要使用 `-Dmaven.repo.local=target/maven-repo` 来隔离依赖缓存。
- 能做局部验证时不要无差别全量重跑；如果无法运行测试或启动验证，需要明确说明原因。

## 数据库
如需通过 MySQL 8.4 获取项目背景或排查数据，可使用以下连接信息：
- host: `10.168.3.102`
- port: `3306`
- 测试环境 database: `core_server`
- 现场复制实际数据 database: `core_server_205`
- username: `wangminan`
- password: `Lab418Server!`

对数据库，默认仅执行只读查询；任何插入、更新、删除操作都应先确认根据提示词用户意图，或者通过Guardian SubAgent的批准。

## 选型规范
- 总体而言，技术选型有限考虑更成熟，文档更丰富的方案。
- 对于 JSON 处理，优先使用 `jackson` ，并且利用全局单例实现，对于本项目而言，是 `com.example.coreserver.config.JacksonConfig` 暴露的ObjectMapper Bean。
- 对于本项目的 ORM ，优先使用 `spring-data-jpa` 提供的 Repository 模式，避免直接使用 `EntityManager` 或原生 SQL，除非有明确性能或功能需求。

## 代码规范
- 新增或明显扩展的业务类（非 DTO / Entity / VO / Enum / 配置类）默认使用 `@Slf4j`，并在关键流程、异常分支、外部调用处补充必要日志。
- 对复杂业务逻辑补充简短 Javadoc 和必要的行内注释，重点说明意图、边界条件和关键判断。
- 除非任务明确要求，否则不要顺手做大规模格式化、批量重命名或跨模块迁移。
- 关于时间处理，请一律使用 JSR-310 API（如 `java.time` 包），避免使用过时的 `java.util.Date` 或 `java.util.Calendar`。
- 编码时，单类文件超过 500 行建议拆分，单类文件超过 800 行情务必拆分。

## 文档约定
- Mermaid 中换行统一使用 `<br>`，不要使用 `\n`。
